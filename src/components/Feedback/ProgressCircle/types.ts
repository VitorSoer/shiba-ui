import { ColorType } from '../../../theme/constants/color';

export interface IProgressCircle {
  progressValue?: number;
  $progressCircleSize?: number;
  $progressCircleRangeColor?: ColorType;
  $progressCircleBaseColor?: ColorType;
}
